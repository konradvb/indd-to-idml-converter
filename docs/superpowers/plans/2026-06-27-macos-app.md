# INDD → IDML Converter macOS App – Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eine native macOS SwiftUI App bauen, die .indd Dateien per Drag & Drop oder Ordnerauswahl in .idml konvertiert, mit Fortschrittsanzeige und Ergebnis.

**Architecture:** SwiftUI App mit 3 Quelldateien. `Converter.swift` kapselt die gesamte AppleScript-Logik (Datei auf Desktop kopieren → InDesign exportiert → zurückverschieben). `ContentView.swift` verwaltet den UI-Zustand und ruft den Converter auf. `App.swift` konfiguriert das Fenster.

**Tech Stack:** Swift 5.9+, SwiftUI, AppKit (NSOpenPanel, NSAppleScript), Xcode 15+, macOS 13+

## Global Constraints

- Ziel-Platform: macOS 13.0+
- Kein Code-Signing, kein Hardened Runtime
- Kein externes Package (kein SPM, kein CocoaPods)
- App-Name: `INDD IDML Converter`
- Bundle Identifier: `de.konradvb.indd-idml-converter`
- Sprache: Deutsch in der UI, Englisch im Code
- Projektordner: `/private/tmp/claude-501/-/7721bbf9-93b8-4e76-9972-9d757b7aa432/scratchpad/indd-to-idml-converter/`

---

### Task 1: Xcode-Projekt anlegen und Grundstruktur erstellen

**Files:**
- Create: `INDDConverter/INDDConverter.xcodeproj`
- Create: `INDDConverter/App.swift`
- Create: `INDDConverter/ContentView.swift`
- Create: `INDDConverter/Converter.swift`

**Interfaces:**
- Produces: Xcode-Projekt mit kompilierbarem Grundgerüst; `ConversionResult` Struct und `Converter` Klasse (Stubs)

- [ ] **Step 1: Xcode-Projekt via `scaffold_macos_project` anlegen**

Nutze das XcodeBuildMCP-Tool `scaffold_macos_project` mit:
```
name: "INDDConverter"
bundle_id: "de.konradvb.indd-idml-converter"
directory: "/private/tmp/claude-501/-/7721bbf9-93b8-4e76-9972-9d757b7aa432/scratchpad/indd-to-idml-converter"
```

- [ ] **Step 2: `App.swift` schreiben**

Inhalt von `INDDConverter/App.swift`:
```swift
import SwiftUI

@main
struct INDDConverterApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowResizability(.contentSize)
    }
}
```

- [ ] **Step 3: `Converter.swift` Stub schreiben**

Inhalt von `INDDConverter/Converter.swift`:
```swift
import Foundation
import AppKit

struct ConversionResult {
    let path: String
    let success: Bool
    let error: String?
}

@MainActor
class Converter: ObservableObject {
    @Published var isRunning = false
    @Published var progress: Double = 0
    @Published var currentFile = ""
    @Published var results: [ConversionResult] = []

    static func inDesignInstalled() -> Bool {
        let fm = FileManager.default
        let appDir = URL(fileURLWithPath: "/Applications")
        let items = (try? fm.contentsOfDirectory(at: appDir, includingPropertiesForKeys: nil)) ?? []
        return items.contains { $0.lastPathComponent.hasPrefix("Adobe InDesign") }
    }

    func findInddFiles(in folderURL: URL) -> [URL] {
        guard let enumerator = FileManager.default.enumerator(
            at: folderURL,
            includingPropertiesForKeys: [.isRegularFileKey],
            options: [.skipsHiddenFiles]
        ) else { return [] }
        return enumerator.compactMap { $0 as? URL }.filter { $0.pathExtension.lowercased() == "indd" }
    }

    func convert(files: [URL]) async {
        isRunning = true
        progress = 0
        results = []
        let total = Double(files.count)

        for (index, file) in files.enumerated() {
            currentFile = file.lastPathComponent
            let result = await convertSingle(file)
            results.append(result)
            progress = Double(index + 1) / total
        }

        isRunning = false
        currentFile = ""
    }

    private func convertSingle(_ url: URL) async -> ConversionResult {
        // Implementierung in Task 2
        return ConversionResult(path: url.path, success: false, error: "Nicht implementiert")
    }
}
```

- [ ] **Step 4: `ContentView.swift` Stub schreiben**

Inhalt von `INDDConverter/ContentView.swift`:
```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("INDD → IDML Converter")
            .padding(40)
    }
}
```

- [ ] **Step 5: Prüfen ob das Projekt baut**

```bash
xcodebuild -project "/private/tmp/claude-501/-/7721bbf9-93b8-4e76-9972-9d757b7aa432/scratchpad/indd-to-idml-converter/INDDConverter/INDDConverter.xcodeproj" \
  -scheme INDDConverter \
  -configuration Debug \
  build 2>&1 | tail -5
```
Erwartete Ausgabe: `** BUILD SUCCEEDED **`

- [ ] **Step 6: Committen**

```bash
cd /private/tmp/claude-501/-/7721bbf9-93b8-4e76-9972-9d757b7aa432/scratchpad/indd-to-idml-converter
git add INDDConverter/
git commit -m "Xcode-Projekt: Grundstruktur mit App, ContentView und Converter Stubs"
```

---

### Task 2: Converter-Logik implementieren

**Files:**
- Modify: `INDDConverter/Converter.swift` — `convertSingle()` implementieren

**Interfaces:**
- Consumes: `ConversionResult`, `Converter` aus Task 1
- Produces: `convertSingle(_ url: URL) async -> ConversionResult` — voll funktionsfähig

- [ ] **Step 1: `convertSingle` in `Converter.swift` implementieren**

Ersetze die `convertSingle`-Methode:
```swift
private func convertSingle(_ url: URL) async -> ConversionResult {
    let desktop = FileManager.default.homeDirectoryForCurrentUser
        .appendingPathComponent("Desktop")
    let tempIndd = desktop.appendingPathComponent("_indd_convert_work.indd")
    let tempIdml = desktop.appendingPathComponent("_indd_convert_work.idml")
    let idmlURL = url.deletingPathExtension().appendingPathExtension("idml")

    // Aufräumen falls Temp-Dateien noch existieren
    try? FileManager.default.removeItem(at: tempIndd)
    try? FileManager.default.removeItem(at: tempIdml)

    do {
        // Datei auf Desktop kopieren (umgeht iCloud-Container-Sandbox)
        try FileManager.default.copyItem(at: url, to: tempIndd)
    } catch {
        return ConversionResult(path: url.path, success: false, error: "Kopieren fehlgeschlagen: \(error.localizedDescription)")
    }

    let script = """
    with timeout of 300 seconds
        tell application "Adobe InDesign 2026"
            set userInteractionLevel to never interact
            set theAlias to POSIX file "\(tempIndd.path)" as alias
            set theDoc to open theAlias
            tell theDoc
                export format InDesign Markup to "\(tempIdml.path)" without showing options
            end tell
            close theDoc saving no
            set userInteractionLevel to interact with all
        end tell
    end timeout
    """

    var error: NSDictionary?
    let appleScript = NSAppleScript(source: script)
    await MainActor.run { _ = appleScript?.executeAndReturnError(&error) }

    try? FileManager.default.removeItem(at: tempIndd)

    if let err = error {
        try? FileManager.default.removeItem(at: tempIdml)
        return ConversionResult(path: url.path, success: false, error: err[NSAppleScript.errorMessage] as? String ?? "AppleScript-Fehler")
    }

    do {
        if FileManager.default.fileExists(atPath: idmlURL.path) {
            try FileManager.default.removeItem(at: idmlURL)
        }
        try FileManager.default.moveItem(at: tempIdml, to: idmlURL)
        return ConversionResult(path: url.path, success: true, error: nil)
    } catch {
        return ConversionResult(path: url.path, success: false, error: "Verschieben fehlgeschlagen: \(error.localizedDescription)")
    }
}
```

- [ ] **Step 2: Entitlements für AppleScript setzen**

In Xcode: Target → Signing & Capabilities → App Sandbox deaktivieren (oder `com.apple.security.automation.apple-events` auf `true` setzen in der `.entitlements` Datei).

Datei `INDDConverter/INDDConverter.entitlements`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <false/>
</dict>
</plist>
```

- [ ] **Step 3: Projekt neu bauen**

```bash
xcodebuild -project "INDDConverter/INDDConverter.xcodeproj" \
  -scheme INDDConverter -configuration Debug build 2>&1 | tail -5
```
Erwartete Ausgabe: `** BUILD SUCCEEDED **`

- [ ] **Step 4: Committen**

```bash
git add INDDConverter/
git commit -m "Converter: AppleScript-Konvertierungslogik mit Desktop-Workaround"
```

---

### Task 3: Vollständige UI in ContentView implementieren

**Files:**
- Modify: `INDDConverter/ContentView.swift` — vollständige UI

**Interfaces:**
- Consumes: `Converter` (ObservableObject), `ConversionResult` aus Task 1/2

- [ ] **Step 1: `ContentView.swift` vollständig schreiben**

```swift
import SwiftUI
import AppKit

struct ContentView: View {
    @StateObject private var converter = Converter()
    @State private var selectedFolder: URL?
    @State private var foundFiles: [URL] = []
    @State private var isDragging = false
    private let inDesignInstalled = Converter.inDesignInstalled()

    var successCount: Int { converter.results.filter(\.success).count }
    var errorCount: Int { converter.results.filter { !$0.success }.count }

    var body: some View {
        VStack(spacing: 0) {
            // InDesign-Warnung
            if !inDesignInstalled {
                HStack(spacing: 8) {
                    Image(systemName: "exclamationmark.triangle.fill")
                        .foregroundColor(.yellow)
                    Text("Adobe InDesign nicht gefunden – bitte installieren.")
                        .font(.callout)
                }
                .frame(maxWidth: .infinity)
                .padding(.vertical, 10)
                .padding(.horizontal, 16)
                .background(Color.yellow.opacity(0.15))
            }

            VStack(spacing: 24) {
                // Titel
                Text("INDD → IDML Converter")
                    .font(.title2.bold())

                // Drop-Zone
                ZStack {
                    RoundedRectangle(cornerRadius: 12)
                        .stroke(
                            isDragging ? Color.accentColor : Color.secondary.opacity(0.4),
                            style: StrokeStyle(lineWidth: 2, dash: [6])
                        )
                        .background(
                            RoundedRectangle(cornerRadius: 12)
                                .fill(isDragging ? Color.accentColor.opacity(0.08) : Color.clear)
                        )
                        .frame(height: 140)

                    VStack(spacing: 10) {
                        Image(systemName: "arrow.down.doc")
                            .font(.system(size: 32))
                            .foregroundColor(.secondary)
                        Text("Ordner hierher ziehen")
                            .foregroundColor(.secondary)
                        Button("Ordner wählen") { pickFolder() }
                            .buttonStyle(.borderedProminent)
                            .controlSize(.small)
                    }
                }
                .onDrop(of: [.fileURL], isTargeted: $isDragging) { providers in
                    handleDrop(providers)
                }
                .disabled(converter.isRunning)

                // Status
                if !foundFiles.isEmpty || converter.isRunning || !converter.results.isEmpty {
                    VStack(spacing: 12) {
                        if converter.results.isEmpty && !converter.isRunning {
                            Text("\(foundFiles.count) .indd \(foundFiles.count == 1 ? "Datei" : "Dateien") gefunden")
                                .foregroundColor(.secondary)
                        }

                        if converter.isRunning {
                            VStack(spacing: 6) {
                                ProgressView(value: converter.progress)
                                Text(converter.currentFile)
                                    .font(.caption)
                                    .foregroundColor(.secondary)
                                    .lineLimit(1)
                                    .truncationMode(.middle)
                            }
                        }

                        if !converter.results.isEmpty && !converter.isRunning {
                            HStack(spacing: 16) {
                                Label("\(successCount) erfolgreich", systemImage: "checkmark.circle.fill")
                                    .foregroundColor(.green)
                                if errorCount > 0 {
                                    Label("\(errorCount) Fehler", systemImage: "xmark.circle.fill")
                                        .foregroundColor(.red)
                                }
                            }
                            .font(.callout.bold())
                        }
                    }
                }

                // Aktions-Button
                if !foundFiles.isEmpty && !converter.isRunning && converter.results.isEmpty {
                    Button("Konvertieren starten") {
                        Task { await converter.convert(files: foundFiles) }
                    }
                    .buttonStyle(.borderedProminent)
                    .controlSize(.large)
                    .disabled(!inDesignInstalled)
                }

                if !converter.results.isEmpty && !converter.isRunning {
                    Button("Neuer Ordner") {
                        foundFiles = []
                        selectedFolder = nil
                        converter.results = []
                        converter.progress = 0
                    }
                    .buttonStyle(.bordered)
                }
            }
            .padding(24)
        }
        .frame(width: 420)
    }

    private func pickFolder() {
        let panel = NSOpenPanel()
        panel.canChooseFiles = false
        panel.canChooseDirectories = true
        panel.allowsMultipleSelection = false
        panel.prompt = "Ordner wählen"
        if panel.runModal() == .OK, let url = panel.url {
            loadFiles(from: url)
        }
    }

    private func handleDrop(_ providers: [NSItemProvider]) -> Bool {
        guard let provider = providers.first else { return false }
        provider.loadItem(forTypeIdentifier: "public.file-url", options: nil) { item, _ in
            guard let data = item as? Data,
                  let url = URL(dataRepresentation: data, relativeTo: nil) else { return }
            DispatchQueue.main.async { self.loadFiles(from: url) }
        }
        return true
    }

    private func loadFiles(from url: URL) {
        selectedFolder = url
        foundFiles = converter.findInddFiles(in: url)
        converter.results = []
        converter.progress = 0
    }
}
```

- [ ] **Step 2: Bauen und visuell in Simulator/Mac prüfen**

```bash
xcodebuild -project "INDDConverter/INDDConverter.xcodeproj" \
  -scheme INDDConverter -configuration Debug build 2>&1 | tail -5
```
Erwartete Ausgabe: `** BUILD SUCCEEDED **`

App starten und prüfen:
- Drop-Zone sichtbar
- "Ordner wählen" Button funktioniert
- InDesign-Banner erscheint nur wenn nicht installiert

- [ ] **Step 3: Committen**

```bash
git add INDDConverter/ContentView.swift
git commit -m "UI: vollständige ContentView mit Drag & Drop, Fortschritt und Ergebnis"
```

---

### Task 4: Release bauen und auf GitHub hochladen

**Files:**
- Keine Code-Änderungen — Build & Packaging

- [ ] **Step 1: Release-Build erstellen**

```bash
xcodebuild -project "INDDConverter/INDDConverter.xcodeproj" \
  -scheme INDDConverter \
  -configuration Release \
  -derivedDataPath /tmp/indd-converter-build \
  build 2>&1 | tail -5
```
Erwartete Ausgabe: `** BUILD SUCCEEDED **`

- [ ] **Step 2: .app finden und ZIP erstellen**

```bash
APP_PATH=$(find /tmp/indd-converter-build -name "INDDConverter.app" -type d | head -1)
echo "App: $APP_PATH"
cd "$(dirname "$APP_PATH")"
zip -r ~/Desktop/INDD-IDML-Converter-v1.0.zip "INDDConverter.app"
echo "ZIP erstellt: ~/Desktop/INDD-IDML-Converter-v1.0.zip"
```

- [ ] **Step 3: GitHub Release erstellen und ZIP hochladen**

```bash
cd /private/tmp/claude-501/-/7721bbf9-93b8-4e76-9972-9d757b7aa432/scratchpad/indd-to-idml-converter
gh release create v1.0 \
  ~/Desktop/INDD-IDML-Converter-v1.0.zip \
  --title "v1.0 – INDD IDML Converter" \
  --notes "## INDD → IDML Converter v1.0

Konvertiert Adobe InDesign (.indd) Dateien in das offene IDML-Format – kompatibel mit Affinity Publisher.

### Voraussetzungen
- macOS 13 oder neuer
- Adobe InDesign installiert (für die Konvertierung)

### Installation
1. ZIP entpacken
2. App in den Programme-Ordner ziehen (optional)
3. Beim ersten Start: Rechtsklick → Öffnen (Gatekeeper-Hinweis bestätigen)

### Verwendung
Ordner auf die App ziehen oder per Button wählen → Konvertieren starten"
```

- [ ] **Step 4: README aktualisieren**

In `README.md` am Anfang einfügen:
```markdown
## Download

[INDD-IDML-Converter-v1.0.zip](https://github.com/konradvb/indd-to-idml-converter/releases/tag/v1.0) — macOS App, kein Adobe nötig zum Öffnen der IDML-Dateien.
```

```bash
git add README.md
git commit -m "README: Download-Link zur macOS App v1.0"
git push
```
